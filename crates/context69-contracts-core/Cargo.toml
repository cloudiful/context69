[package]
name = "context69-contracts-core"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Core shared contracts (common, pagination, errors, Visibility, TaskRef) for Context69."
publish = false

[dependencies]
anyhow.workspace = true
chrono = { workspace = true, features = ["serde"] }
schemars = { workspace = true, features = ["chrono04", "uuid1"] }
serde = { workspace = true, features = ["derive"] }
serde_json.workspace = true
utoipa = { workspace = true, features = [ "chrono", "uuid" ] }
uuid = { workspace = true, features = [ "serde", "v4", "v5" ] }
